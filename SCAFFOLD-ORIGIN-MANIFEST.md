# Scaffold origin manifest

`SCAFFOLD-ORIGIN-MANIFEST.json` records file sizes and hashes from the original
imported scaffold. It is historical provenance, not an integrity manifest for
the current repository tree; implementation changes are intentionally not
reflected in it.
