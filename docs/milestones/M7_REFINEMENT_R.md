# M7 — Draft then refine

Add outer refinement.

```text
iteration 0: causal draft
iteration >=1: global refinement
```

First use externally specified selectors.

Operator:

```text
select
erase/corrupt target (+ configured neighbour)
regenerate
commit
```

## Controls

- causal regeneration;
- global regeneration;
- redirected/non-propagating control where applicable;
- random selector;
- oracle selector on synthetic tasks.

## Gate

Global/later-context refinement provides a reproducible causal benefit on the selected reconstruction problem, with non-target preservation measured explicitly.
